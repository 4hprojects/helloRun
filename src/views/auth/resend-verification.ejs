<!-- filepath: c:\Users\Kayla Ryhs\Desktop\PersonalProjects\helloRun\hellorun\src\views\auth\resend-verification.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../layouts/nav', { user: null }) %>

  <main class="auth-page">
    <div class="auth-container">
      <h1>Resend Verification Email</h1>
      
      <% if (success) { %>
        <div class="success">
          ✅ Verification email sent to <strong><%= email %></strong>
          <p style="margin-top: 10px;">Please check your inbox and spam folder.</p>
        </div>
        <a href="/login" class="btn" style="margin-top: 20px;">Go to Login</a>
      <% } else { %>
        <% if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>
        
        <p>Enter your email address to receive a new verification link.</p>
        
        <form action="/resend-verification" method="POST">
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<%= email || '' %>" required>
          </div>
          <button type="submit" class="btn">Send Verification Email</button>
        </form>
        
        <p><a href="/login">← Back to Login</a></p>
      <% } %>
    </div>
  </main>

  <%- include('../layouts/footer') %>
</body>
</html>