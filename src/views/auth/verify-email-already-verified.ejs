<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../layouts/head', { title: 'Email Already Verified - helloRun' }) %>
  <link rel="stylesheet" href="/css/email-verification.css">
</head>
<body>
  <%- include('../layouts/nav') %>
  
  <main class="auth-page">
    <div class="auth-container">
      <div class="auth-card">
        <div class="auth-icon success">
          <i data-lucide="check-circle"></i>
        </div>
        
        <h1>Email Already Verified</h1>
        <p class="auth-subtitle">Your email has already been verified. You're all set!</p>
        
        <div class="auth-info">
          <p><strong>Email:</strong> <%= email %></p>
          <% if (firstName) { %>
            <p class="mt-20"><strong>Welcome back, <%= firstName %>! 👋</strong></p>
          <% } %>
        </div>
        
        <div class="auth-actions">
          <a href="/login" class="btn btn-full">Go to Login</a>
          <a href="/" class="btn btn-secondary btn-full">Back to Home</a>
        </div>
      </div>
    </div>
  </main>
  
  <%- include('../layouts/footer') %>
  
  <script src="/js/main.js"></script>
  <script>
    lucide.createIcons();
  </script>
</body>
</html>