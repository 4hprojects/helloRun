<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify Your Email - HelloRun</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://unpkg.com/lucide@latest"></script>
  <style>
    .verify-sent-page {
      min-height: calc(100vh - 80px);
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #FFF5EB 0%, #FFE8D6 100%);
      position: relative;
      overflow: hidden;
      padding: 20px;
    }

    /* Animated background elements */
    .verify-sent-page::before,
    .verify-sent-page::after {
      content: '';
      position: absolute;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(250, 154, 75, 0.1) 0%, rgba(224, 164, 106, 0.1) 100%);
      animation: float 8s ease-in-out infinite;
    }

    .verify-sent-page::before {
      width: 350px;
      height: 350px;
      top: -150px;
      right: -100px;
      animation-delay: 0s;
    }

    .verify-sent-page::after {
      width: 450px;
      height: 450px;
      bottom: -200px;
      left: -150px;
      animation-delay: 4s;
    }

    @keyframes float {
      0%, 100% { transform: translate(0, 0) rotate(0deg); }
      50% { transform: translate(20px, -20px) rotate(180deg); }
    }

    .verify-sent-container {
      position: relative;
      z-index: 1;
      width: 100%;
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 45% 55%;
      background: white;
      box-shadow: 0 0 60px rgba(0, 0, 0, 0.15);
      border-radius: 20px;
      overflow: hidden;
      max-height: calc(100vh - 120px);
    }

    /* LEFT PANEL - Illustration */
    .verify-left-panel {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      padding: 40px 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: white;
      position: relative;
      overflow: hidden;
    }

    .verify-left-panel::before,
    .verify-left-panel::after {
      content: '';
      position: absolute;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
    }

    .verify-left-panel::before {
      width: 250px;
      height: 250px;
      top: -80px;
      left: -80px;
    }

    .verify-left-panel::after {
      width: 200px;
      height: 200px;
      bottom: -60px;
      right: -60px;
    }

    .verify-illustration {
      position: relative;
      z-index: 1;
      margin-bottom: 24px;
    }

    .verify-icon {
      width: 120px;
      height: 120px;
      margin: 0 auto;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      animation: scaleIn 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
      border: 3px solid rgba(255, 255, 255, 0.3);
    }

    @keyframes scaleIn {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .verify-icon::before {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      animation: pulse 2s ease-out infinite;
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
        opacity: 0.3;
      }
      100% {
        transform: scale(1.5);
        opacity: 0;
      }
    }

    .verify-icon i {
      color: white;
      width: 60px;
      height: 60px;
      stroke-width: 2;
      position: relative;
      z-index: 1;
      animation: mailFloat 3s ease-in-out infinite;
    }

    @keyframes mailFloat {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    .left-panel-content h1 {
      font-family: 'Poppins', sans-serif;
      font-size: 2rem;
      font-weight: 800;
      margin-bottom: 12px;
      animation: slideUp 0.6s ease-out 0.2s both;
    }

    .left-panel-content p {
      font-size: 1rem;
      line-height: 1.5;
      opacity: 0.95;
      animation: slideUp 0.6s ease-out 0.3s both;
      margin-bottom: 0;
    }

    .email-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
      padding: 10px 16px;
      border-radius: 50px;
      font-weight: 600;
      font-size: 0.875rem;
      margin-top: 20px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      animation: slideUp 0.6s ease-out 0.4s both;
      max-width: 90%;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .email-badge i {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* RIGHT PANEL - Content */
    .verify-right-panel {
      padding: 40px 35px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      background: white;
      animation: slideIn 0.6s ease-out;
      overflow-y: auto;
      max-height: calc(100vh - 120px);
    }

    /* Custom scrollbar for right panel */
    .verify-right-panel::-webkit-scrollbar {
      width: 6px;
    }

    .verify-right-panel::-webkit-scrollbar-track {
      background: #f1f1f1;
      border-radius: 10px;
    }

    .verify-right-panel::-webkit-scrollbar-thumb {
      background: #d1d5db;
      border-radius: 10px;
    }

    .verify-right-panel::-webkit-scrollbar-thumb:hover {
      background: #9ca3af;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateX(30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .verify-right-panel h2 {
      font-family: 'Poppins', sans-serif;
      font-size: 1.5rem;
      font-weight: 700;
      color: #1a1a1a;
      margin-bottom: 8px;
      animation: slideUp 0.6s ease-out 0.3s both;
    }

    .verify-subtitle {
      font-size: 0.9375rem;
      color: #666;
      line-height: 1.5;
      margin-bottom: 24px;
      animation: slideUp 0.6s ease-out 0.4s both;
    }

    /* Steps section */
    .verify-steps {
      margin-bottom: 20px;
      animation: slideUp 0.6s ease-out 0.5s both;
    }

    .verify-steps h3 {
      font-size: 1rem;
      font-weight: 600;
      color: #1a1a1a;
      margin-bottom: 14px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .verify-steps h3 i {
      width: 20px;
      height: 20px;
      color: #3b82f6;
    }

    .steps-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      gap: 10px;
    }

    .steps-list li {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 12px;
      background: #f9fafb;
      border-radius: 10px;
      font-size: 0.875rem;
      color: #4b5563;
      border-left: 3px solid #3b82f6;
      transition: all 0.3s ease;
    }

    .steps-list li:hover {
      background: #eff6ff;
      transform: translateX(4px);
    }

    .step-number {
      width: 24px;
      height: 24px;
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 0.75rem;
      flex-shrink: 0;
    }

    /* Alert box */
    .alert-box {
      background: #fef3c7;
      border: 2px solid #fbbf24;
      border-radius: 10px;
      padding: 12px;
      display: flex;
      align-items: flex-start;
      gap: 10px;
      margin-bottom: 20px;
      animation: slideUp 0.6s ease-out 0.6s both;
    }

    .alert-box i {
      width: 20px;
      height: 20px;
      color: #d97706;
      flex-shrink: 0;
      margin-top: 2px;
    }

    .alert-box p {
      margin: 0;
      font-size: 0.875rem;
      color: #92400e;
      line-height: 1.4;
    }

    .alert-box strong {
      font-weight: 600;
      color: #78350f;
    }

    /* Action buttons */
    .verify-actions {
      display: flex;
      flex-direction: column;
      gap: 10px;
      animation: slideUp 0.6s ease-out 0.7s both;
    }

    .btn-primary {
      background: linear-gradient(135deg, #FA9A4B 0%, #E0A46A 100%);
      color: white;
      padding: 14px 28px;
      border-radius: 10px;
      font-weight: 600;
      font-size: 1rem;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      box-shadow: 0 4px 12px rgba(250, 154, 75, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(250, 154, 75, 0.4);
    }

    .btn-primary i {
      width: 18px;
      height: 18px;
    }

    .btn-secondary {
      background: transparent;
      color: #666;
      padding: 12px 28px;
      border-radius: 10px;
      font-weight: 500;
      font-size: 0.9375rem;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      transition: all 0.3s ease;
      border: 2px solid #e5e7eb;
    }

    .btn-secondary:hover {
      background: #f9fafb;
      border-color: #d1d5db;
    }

    .btn-secondary i {
      width: 16px;
      height: 16px;
    }

    .resend-link {
      text-align: center;
      margin-top: 16px;
      font-size: 0.875rem;
      color: #666;
      animation: slideUp 0.6s ease-out 0.8s both;
    }

    .resend-link a {
      color: #3b82f6;
      font-weight: 600;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .resend-link a:hover {
      color: #2563eb;
      text-decoration: underline;
    }

    /* MOBILE RESPONSIVENESS */
    @media (max-width: 968px) {
      .verify-sent-page {
        padding: 15px;
      }

      .verify-sent-container {
        grid-template-columns: 1fr;
        border-radius: 16px;
        max-height: none;
      }

      .verify-left-panel {
        padding: 32px 20px;
        min-height: auto;
      }

      .verify-icon {
        width: 100px;
        height: 100px;
      }

      .verify-icon i {
        width: 50px;
        height: 50px;
      }

      .left-panel-content h1 {
        font-size: 1.75rem;
      }

      .left-panel-content p {
        font-size: 0.9375rem;
      }

      .email-badge {
        font-size: 0.8125rem;
        padding: 8px 14px;
        margin-top: 16px;
      }

      .verify-right-panel {
        padding: 32px 20px;
        max-height: none;
        overflow-y: visible;
      }

      .verify-right-panel h2 {
        font-size: 1.375rem;
      }

      .verify-subtitle {
        font-size: 0.875rem;
        margin-bottom: 20px;
      }

      .verify-steps {
        margin-bottom: 16px;
      }

      .verify-steps h3 {
        font-size: 0.9375rem;
      }

      .steps-list {
        gap: 8px;
      }

      .steps-list li {
        padding: 10px;
        font-size: 0.8125rem;
      }

      .step-number {
        width: 22px;
        height: 22px;
        font-size: 0.6875rem;
      }

      .alert-box {
        padding: 10px;
        margin-bottom: 16px;
      }

      .alert-box i {
        width: 18px;
        height: 18px;
      }

      .alert-box p {
        font-size: 0.8125rem;
      }

      .btn-primary,
      .btn-secondary {
        font-size: 0.9375rem;
        padding: 12px 24px;
      }

      .resend-link {
        margin-top: 12px;
        font-size: 0.8125rem;
      }
    }

    @media (max-width: 480px) {
      .verify-sent-page {
        padding: 10px;
      }

      .verify-left-panel {
        padding: 28px 16px;
      }

      .verify-icon {
        width: 90px;
        height: 90px;
      }

      .verify-icon i {
        width: 45px;
        height: 45px;
      }

      .left-panel-content h1 {
        font-size: 1.5rem;
      }

      .verify-right-panel {
        padding: 28px 16px;
      }

      .verify-right-panel h2 {
        font-size: 1.25rem;
      }
    }
  </style>
</head>
<body>
  <%- include('../layouts/nav') %>
  
  <main class="verify-sent-page">
    <div class="verify-sent-container">
      
      <!-- LEFT PANEL - Illustration -->
      <div class="verify-left-panel">
        <div class="verify-illustration">
          <div class="verify-icon">
            <i data-lucide="mail"></i>
          </div>
        </div>
        
        <div class="left-panel-content">
          <h1>Check Your Email</h1>
          <p>We've sent a verification link to complete your registration</p>
          
          <% if (email) { %>
          <div class="email-badge">
            <i data-lucide="send"></i>
            <%= email %>
          </div>
          <% } %>
        </div>
      </div>

      <!-- RIGHT PANEL - Instructions -->
      <div class="verify-right-panel">

        <p class="verify-subtitle">
          We've sent a verification email to your inbox. Please follow these steps to activate your account:
        </p>

        <!-- Steps -->
        <div class="verify-steps">
          <h3>
            <i data-lucide="list-checks"></i>
            Next Steps
          </h3>
          <ul class="steps-list">
            <li>
              <span class="step-number">1</span>
              <span>Open your email inbox and look for our verification email</span>
            </li>
            <li>
              <span class="step-number">2</span>
              <span>Click the verification link in the email (valid for 48 hours)</span>
            </li>
            <li>
              <span class="step-number">3</span>
              <span>Your account will be activated and ready to use</span>
            </li>
          </ul>
        </div>

        <!-- Alert Box -->
        <div class="alert-box">
          <i data-lucide="alert-circle"></i>
          <p>
            <strong>Can't find the email?</strong><br>
            Check your spam or junk folder. If you still don't see it, you can request a new verification email.
          </p>
        </div>

        <!-- Action Buttons -->
        <div class="verify-actions">
          <a href="/login" class="btn-primary">
            <i data-lucide="log-in"></i>
            Go to Login
          </a>
          <a href="/" class="btn-secondary">
            <i data-lucide="home"></i>
            Back to Home
          </a>
        </div>

        <!-- Resend Link -->
        <p class="resend-link">
          Didn't receive the email? <a href="/resend-verification">Resend verification email</a>
        </p>
      </div>

    </div>
  </main>
  
  <%- include('../layouts/footer') %>
  
  <script src="/js/main.js"></script>
  <script>
    lucide.createIcons();
  </script>
</body>
</html>