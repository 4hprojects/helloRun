# Implementation Records Awaiting Live Verification

The code described here is implemented and repository-verified, but its
remaining integration or production checks cannot safely run against the
historically production-connected local environment.

- [Admin data exports](admin-data-exports.md)
- [Admin mutation rate limiting](admin-mutation-rate-limiting.md)

Move a record into the main implementation index only after verification is
performed in an explicitly approved environment and the result is recorded.
